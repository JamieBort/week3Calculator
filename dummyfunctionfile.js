// declare variables
let array = [1 , + , 3];
let parameter1 = array[0];
let operation = array[1];
let parameter2 = array[2];
let sum;

function statement(a, b, c){
if (operation === '/' && parameter2 === 0) {
  // "operation is undefined"
  // console.log("The operation is undefined. You cannot divided by zero.");
  return "Mathematically speaking, the operation is undefined. You cannot divided by zero.";
} else if (array[1] === '+') {
  let sum = parameter1 + parameter2
} else if (array[1] === '-') {
  let sum = parameter1 - parameter2
} else if (array[1] === '+') {
  let sum = parameter1 + parameter2
}
};

statement(1 , + , 3);
console.log(statement(1 , + , 3));
